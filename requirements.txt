# ComfyUI-OmniX Requirements
# Core dependencies for OmniX panorama generation and perception

# PyTorch and related (usually installed with ComfyUI)
torch>=2.0.0
torchvision>=0.15.0

# Model loading and inference
safetensors>=0.4.0
accelerate>=0.24.0
huggingface_hub>=0.20.0
diffusers>=0.34.0
transformers>=4.37.0
peft>=0.10.0

# Utilities
numpy>=1.24.0
pillow>=10.0.0
scipy>=1.11.0

# Optional: for depth map visualization
matplotlib>=3.7.0

# Optional: for advanced features
einops>=0.7.0
omegaconf>=2.3.0
